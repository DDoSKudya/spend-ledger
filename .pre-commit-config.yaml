repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.17
    hooks:
      - id: ruff
        args: [--config, ruff.toml]
        files: ^services/
      - id: ruff-format
        args: [--config, ruff.toml]
        files: ^services/
